/// مستودع GitHub الذي يُقرأ منه أحدث إصدار.
///
/// الخدمة تستعلم `releases/latest` مباشرةً عبر GitHub API، بلا قاعدة بيانات
/// ولا خادم وسيط: الوسم (tag) هو رقم الإصدار، والملفات المرفقة به هي
/// المُثبِّتات. فلا شيء يُملأ يدوياً عند كل إصدار، ولا شيء يمكن أن يفترق عن
/// الواقع.
///
/// المستودع يجب أن يكون عاماً — ملفات إصدار في مستودع خاص تُعيد 404 لمن
/// يحاول تنزيلها بلا تسجيل دخول، ونداء API بلا مصادقة لا يراها أصلاً.
const String kGithubRepoOwner = 'hassnmo998-del';
const String kGithubRepoName  = 'mihrab-app';

/// رقم الإصدار الحالي للتطبيق (مطابق لـ pubspec.yaml)
const String kCurrentAppVersion = '1.0.4';
